// console.log(null == undefined); // true   // null and undefined are the same value but different types
// console.log(null === undefined); // false  // null and undefined are two different types
let age = 100;
let result = age > 18 ? "adult" : "child";

console.log(result);
