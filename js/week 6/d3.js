// let name = "Shahid";

// for (let val of name) {
//     console.log(val);
//

// console.log(`my name is ${name}`);          // string interpolation
// touppercase() toLowerCase() trim()
// console.log(name.toUpperCase());
//  slice()

// str.concat(str2)

// CharAt()
// prompt the user to enter their full name, generate a username for them based on input, start username at @ followed by fullname and ending at fullname length

// let str = prompt("Enter Your Name: ");

// let val = "@";
// let username = val.concat(str);
// console.log(`${username}${str.length}`);

// Strings are immutable    // arrays are mutable

// for a given array withmarks of stds 84,97.44,37,76,60. find the average of the marks.

// let arr = [84, 97, 44, 37, 76, 60];
// let sum =
